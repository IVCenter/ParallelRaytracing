package network;

public class NetworkMessageListener implements MessageListener {
	
	/*
	 * A network based message listener
	 */
	/*
	 * TODO:
	 * 
	 * 		-Open a socket
	 * 		-Use a executor service and a worker class to handle requests on the socket
	 * 			-Parse the message from the request
	 * 			-Send it to a given handler
	 */

}
